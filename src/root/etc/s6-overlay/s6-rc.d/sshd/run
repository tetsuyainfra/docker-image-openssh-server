#!/command/execlineb -P
# フォアグラウンドで sshd を exec
importas DEBUG DEBUG

define -s SSHD_RUN     "/usr/sbin/sshd -f /config/sshd_config -D -e"
define -s SSHD_RUN_D   "/usr/sbin/sshd -f /config/sshd_config -D -e -d"

# # Redirect stderr to stdout.
fdmove -c 2 1

$SSHD_RUN
# $SSHD_RUN_D